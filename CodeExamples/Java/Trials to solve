Сбалансированные скобки:

Метод isBalanced():
Принимает строку, состоящую только из ( и ), и проверяет, является ли строка корректной.
Пустая строка корректна.

Требования к строкам:
- у каждой ( есть ) 
- ) не идёт впереди (, т.е. )( недопустимо


Преобразование DNA в RNA:
Метод dnaToRna(): 
Принимает на вход цепь ДНК и возвращает соответствующую цепь РНК (совершает транскрипцию РНК).

ДНК: A C G T
РНК: A C G U

Если во входном параметре нет ни одного нуклеотида (т.е. передана пустая строка), то функция должна вернуть пустую строку. 
Если в переданной цепи ДНК встретится "незнакомый" нуклеотид (не являющийся одним из четырех перечисленных выше), то функция должна вернуть null. 

Разница углов:
Метод diff():
Принимает два угла (integer), каждый от 0 до 360, и возвращает разницу между ними. Функция должна вернуть наименьшее значение.
diff(0, 45) === 45; 

Фибоначчи:
Метод fib(), находящий положительные числа Фибоначчи. Аргументом является порядковый номер числа.

Складываем цифры:
Метод addDigits():
принимает на вход неотрицательное целое число и возвращает другое число, полученное из первого следующим преобразованием: 
Итеративно сложите все входящие в него цифры до тех пор пока, не останется одна цифра. 

Пример: Число 38: 3 + 8 = 11, 1 + 1 = 2
